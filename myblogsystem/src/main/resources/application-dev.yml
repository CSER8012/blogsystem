spring:
  thymeleaf:
    mode: HTML
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/MyBlog?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: mysql2018!
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
  logging:
    level:
     root: info
     com.example: info
    file: e:/log/blogdev.log
server:
  port: 8080
